how to push your code to remote local repository from terminal 

git remote add origin "path for your repository"
git branch -M "branch name on which you want to push"
git push -u "above branch name"
